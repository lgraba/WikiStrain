= simple_form_for @article do |f|
	%p= f.collection_select :category_id, Category.all, :id, :name, {prompt: "Choose a Category"}
	= f.input :title
	= f.input :content
	= f.submit "Create Article", class: "btn btn-default pull-right"